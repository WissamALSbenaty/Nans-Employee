
// ignore_for_file: constant_identifier_names

enum ToastType {  success, error }

enum SortType{none,priceHighestToLowest,priceLowestToHighest,newestToOldest,oldestToNewest,}

enum TextFieldPriority{none,optional,required, }

enum ShowType{tile,grid,card,carousel}

enum OrderStatus{ Pending,Approved,Delivering,Delivered,Rejected,CanceledByAdmin,CanceledByClient, }

enum CartType{ Normal,Daily }

enum HomePageType{explore,discount}

enum ChipType{vendor,category,brand}

enum ApplicationState{stable,canUpdate,mustUpdate}

enum VariantKeyContentType{Text,Number,Color}

enum VerificationReason{ForgetPassword,VerifyAccount,ChangePhoneNumber}

enum DiscountType{Percentage,Fixed}
